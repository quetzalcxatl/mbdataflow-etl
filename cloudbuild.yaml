# ============================================================
# MBDataFlow_ETL — Cloud Build configuration
# Builds the shared pipeline image and pushes to Artifact Registry.
# ============================================================

substitutions:
  _REGION: us-central1
  _REPO: mbdataflow
  _IMAGE: etl-pipelines
  _SHA: latest   # override via --substitutions=_SHA=<short-sha>

steps:
  # ── Step 1: Build the image ──────────────────────────────
  - name: gcr.io/cloud-builders/docker
    id: build
    args:
      - build
      - --tag=$_REGION-docker.pkg.dev/$PROJECT_ID/$_REPO/$_IMAGE:$_SHA
      - --tag=$_REGION-docker.pkg.dev/$PROJECT_ID/$_REPO/$_IMAGE:latest
      - --cache-from=$_REGION-docker.pkg.dev/$PROJECT_ID/$_REPO/$_IMAGE:latest
      - .

  # ── Step 2: Push both tags ───────────────────────────────
  - name: gcr.io/cloud-builders/docker
    id: push-sha
    args:
      - push
      - $_REGION-docker.pkg.dev/$PROJECT_ID/$_REPO/$_IMAGE:$_SHA
    waitFor: [build]

  - name: gcr.io/cloud-builders/docker
    id: push-latest
    args:
      - push
      - $_REGION-docker.pkg.dev/$PROJECT_ID/$_REPO/$_IMAGE:latest
    waitFor: [build]

images:
  - $_REGION-docker.pkg.dev/$PROJECT_ID/$_REPO/$_IMAGE:$_SHA
  - $_REGION-docker.pkg.dev/$PROJECT_ID/$_REPO/$_IMAGE:latest

options:
  logging: CLOUD_LOGGING_ONLY